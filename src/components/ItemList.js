const ItemList = (items) => {
  console.log(items);
  return (
    <div>
      {/* {items.map((items) => (
        <li key={items.card.info.id}></li>
      ))} */}
    </div>
  );
};

export default ItemList;
